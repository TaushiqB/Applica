import { defineConfig } from 'vite';

export default defineConfig({
  server: {
    port: 5173, // Port to run the dev server (default is 5173)
  },
  // If you have additional config, you can add it here
});
